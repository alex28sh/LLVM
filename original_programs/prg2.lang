def fact(int a, int b) {
    while (a != 0 && b != 0) {
        if (a > b) {
            a := a % b;
        } else {
            b := b % a;
        }
    }
    return a + b;
}

def main() {
    int a;
    int b;
    read a;
    read b;
    write fact(a, b);
    return 0;
}